{{- if .Values.namespace.create -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "dashboard-project.tools.namespace" . }}
  annotations:
    "helm.sh/resource-policy": "keep"
  labels:
    name: {{ include "dashboard-project.tools.namespace" . }}
{{- end }}