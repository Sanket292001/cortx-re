apiVersion: v1
kind: Secret
metadata:
  name: dashboard-elasticsearch-es-elastic-user
  namespace: {{ include "elastic-project.tools.namespace" . }}
  labels:
    {{- include "elastic-project.labels" . | nindent 4 }}
type: Opaque
stringData:
  elastic: "{{ required "Elastic-User Password Is Required" .Values.elastic_user.password }}"
